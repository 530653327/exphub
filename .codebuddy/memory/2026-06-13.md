# 2026-06-13 工作日志

## 回退待办看板(Kanban)功能

- 应要求完全回退看板功能，revert 了三个提交：
  - `b2ef42a` feat: 后台新增待办看板(Kanban)页面
  - `8da85f5` fix: 在所有页面侧边栏添加"待办看板"菜单项
  - `0f985a6` fix: 将看板JS移入th:fragment内
- 回退提交: `96deb92`
- 删除: `TodoController.java`, `kanban/index.html`, 10个模板文件的菜单项
- 已推送并部署到生产环境

## 新增 get_experience_history MCP 工具

- 新增 `get_experience_history` MCP 工具，允许 AI 助手读取经验的某个历史版本的完整内容
- 用法：`get_experience_history(id=?, version=?)` — version 可选，不传则列出所有历史版本
- 实现：调用 `DocService.getVersions()` 从 `doc_versions` 表查询版本快照
- 有内容截断保护（同 `get_experience_detail` 的 4000 字限制）
- 注意：误将已 revert 的 `kanban/index.html` 带入了提交，随后的 `d4682c2` 删除修复
